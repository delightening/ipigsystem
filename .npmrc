# 供應鏈防護：不執行任何 npm lifecycle script（preinstall / install / postinstall / prepare）。
#
# 起因：2026-08-04 Shai-Hulud 蠕蟲經 keyv 維護者的 GitHub 帳號被攻破而擴散，波及 868 個套件。
# 惡意碼藏在安裝期 hook，只要跑一次 npm install 就會竊取 npm / GitHub / AWS / K8s / Vault 憑證，
# 再借受害者身分感染下一位維護者。關掉 lifecycle script 是對這類攻擊成本最低的結構性阻斷。
#
# 為什麼只放 repo root：frontend/ 走 pnpm 10（見 frontend/package.json 的 packageManager），
# pnpm 10 預設就不執行依賴的 lifecycle script，且未設 onlyBuiltDependencies 允許清單，
# 該路徑已受保護。repo root 這棵樹走 npm，是本 repo 唯一還會跑安裝期 script 的入口。
#
# ⚠️ 已知副作用（npm 11.6.2 + Node 24 實測）：本設定同時會擋掉本專案自己的 "prepare": "husky"，
#    所以 npm install 不再自動安裝 .husky/ 的 git hooks（pre-commit / pre-push 等 5 支）。
#    fresh clone 後請手動補跑一次（顯式 npm run 不受 ignore-scripts 影響，已實測）：
#        npm run prepare
#
# 若日後某個依賴確實需要安裝期編譯，不要移除本設定，改在該次安裝單獨覆寫：
#        npm install --ignore-scripts=false
ignore-scripts=true
