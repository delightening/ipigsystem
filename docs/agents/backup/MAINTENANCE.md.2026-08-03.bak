# 制度檔維護協議（MAINTENANCE）

> 何時讀本檔：要修改 CLAUDE.md、AGENTS.md、或 `docs/agents/` 下任何檔案之前；踩坑後要記錄教訓時。
> 撰寫：2026-07-04（Fable 5）。本協議的目的：讓制度隨經驗成長，同時防止弱模型把制度改壞。

## 1. 修改權限分級

| 動作 | 授權 |
|---|---|
| 在 RULES_BACKEND / RULES_FRONTEND / DOCS_PROTOCOL 補充**事實性**內容（新踩的坑、新指令陷阱、新表格一行） | 自行改，commit message 說明依據 |
| 修正制度檔中**可驗證的錯誤**（路徑不存在、工具名打錯、指令已失效） | 自行改，但先實際驗證（跑一次 / Glob 確認），回報中說明 |
| 更新 CLAUDE.md「環境事實」段（新增一條踩坑事實） | 自行改 |
| 改 CLAUDE.md「授權現況」段（常設授權 / 必問紅線） | **必問使用者**——這段是權力邊界，只有使用者能動 |
| 改 DISPATCH / JUDGMENT / TEMPLATES 的**判準或門檻**（升降級次數、模型選擇表、驗收標準） | **必問使用者**，並附「現行條文 → 建議條文 → 依據的具體事件」 |
| 刪除任何制度條文 | **必問使用者**（弱模型最常見的錯誤是把「我這次不需要」當「過時」） |
| 改 AGENTS.md | 不改。它是指標檔，內容永遠只有指向 CLAUDE.md |
| 改 `~/.claude/CLAUDE.md`（RTK 全域規則）、settings.json、hooks | **必問使用者** |

**修改任何制度檔前**：複製一份到 `docs/agents/backup/{檔名}.{YYYY-MM-DD}.bak`（同日多次改只留當日第一份）。

## 2. 踩坑教訓寫回哪裡（決策樹）

踩了坑 → 問「這個教訓在什麼時候需要被再次看到？」

1. **每個 session 都可能撞**（指令陷阱、環境行為）→ CLAUDE.md §環境事實，一行。
2. **只在做某類任務時撞**（backend / frontend / 文件）→ 對應 RULES_*.md 的相關小節，附日期。
3. **關於派工 / 驗收方式**的教訓 → 先寫進記憶（metadata.type: feedback），累積 2 次同型教訓
   再提議改 DISPATCH/JUDGMENT（改判準要問，見 §1）。
4. **專案狀態 / 一次性事件**（某功能落地、某 PR 關閉）→ 記憶（type: project），不進制度檔。
5. **使用者糾正了你的行為** → 記憶（type: feedback），含 Why 與 How to apply。

格式：一條教訓 = 一行「現象 → 正確做法」＋（若有）事故日期。禁止把整段事故經過貼進制度檔。

## 3. 記憶（MEMORY.md）維護規範

- 索引行格式：`- [標題](檔名.md) — hook`，**hook ≤80 字元**。細節放記憶檔內文，不放索引。
- 寫新記憶前先搜同主題舊記憶：有 → 更新舊檔，不開新檔。發現記憶已錯 → 直接刪（檔案+索引行）。
- 記憶內提到的檔案 / 函式 / 旗標，引用前先驗證仍存在（記憶反映寫下當時的狀態）。
- **每月一次**（或索引 >80 行時）：掃一遍索引，把「已完結的專案狀態」記憶合併或刪除。
  判斷已完結：記憶自己寫了「已全數落地 / 已解決 / 已放棄」。

## 4. 精簡觸發條件（防制度檔自然膨脹）

| 檔案 | 觸發 | 動作 |
|---|---|---|
| CLAUDE.md | >150 行 | 把最少被引用的段落抽到 docs/agents/，正文留路由 |
| RULES_*.md | >250 行 | 拆小節或把低頻內容移到獨立檔，路由表更新 |
| MEMORY.md 索引 | >80 行 | 依 §3 清理 |
| DISPATCH / JUDGMENT / TEMPLATES | 原則上不長大——新增判準要問使用者（§1） |

精簡 = 移動或合併，不是刪除；刪除要問（§1）。

## 5. 制度衝突處理流程

發現兩條規則打架（本檔 vs CLAUDE.md vs 記憶 vs 舊 docs）：
1. 按 CLAUDE.md §衝突裁決順序決定當下行為（本次指示 > 較新的使用者決定 > 其他）。
2. 當下任務結束後，把衝突寫成一則記憶（type: feedback，命名 `conflict-{主題}`），
   內容：兩條條文原文 + 你的裁決 + 依據。
3. 下次與使用者互動時提出，由使用者裁定後修文件（贏的留、輸的刪或加註日期作廢）。
4. 不要 silent 兩邊都不改——沒修掉的衝突每個 session 都會再燒一次判斷成本。

## 6. 制度健康檢查（輕量，不是儀式）

觸發時機：使用者說「檢查制度」，或你在一個 session 內第 2 次發現制度檔與現實不符。
動作：派一個 sonnet agent 用 `TEMPLATES.md` §6 驗收模板掃全部制度檔，找：
路徑失效、與 repo 現狀矛盾、互相打架的條文。產出清單交使用者，不自行大改。
