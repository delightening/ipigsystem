# 文件記錄規則（TODO / PROGRESS / DESIGN）

> 何時讀本檔：要更新 `docs/TODO.md`、`docs/PROGRESS.md`、`DESIGN.md`，或完成一項 TODO 項目要記錄時。
> 內容抽自 2026-07-04 之前的 CLAUDE.md（原文備份：`docs/agents/backup/CLAUDE.md.2026-07-04.bak`）。

## 核心原則

- 時間性內容一律**反向時間序**（新→舊）。
- 同一事件只詳細記錄一次，其他位置用摘要或指標。
- 每個檔案只做一件事，職責不重疊。
- 規格類文件（spec）待討論完全敲定後一次產出，不逐輪 commit 半成品。

## 各檔案職責

| 檔案 | 唯一職責 | 不該放 |
|------|---------|--------|
| `docs/TODO.md` | 任務追蹤（待辦 + **仍含未完成項的輪次**；整輪完成者移封存） | 變更日誌（已封存）、全 [x] 的舊輪次 |
| `docs/PROGRESS.md` | 完成度概覽（§1-8）+ 唯一變更日誌（§9，近 ~2 月；更舊移封存） | 任務狀態追蹤 |
| `docs/archive/` | 已封存的完結內容（`TODO_done.md` 全完成輪次、`CHANGELOG_*.md` 舊 §9 日誌） | 仍有未完成項的內容 |
| `DESIGN.md` | 設計系統規範 + 設計決策日誌 | 任務、進度 |

## 完成 TODO 項目後的記錄流程（依序）

1. **TODO.md**：項目標 `[x]`，更新「待辦統計」數量。
2. **PROGRESS.md §9**：新增一筆變更紀錄（格式見下）。
3. **DESIGN.md Decisions Log**：僅涉及設計/架構決策時新增。
4. 不在 TODO.md 變更紀錄區新增條目（該區已封存）。

## TODO.md 撰寫規範

章節順序（固定）：
```
禁止事項 → P0…P5 → 歷史改善計畫（P2-R3、P0-P2改進、R4-100）
→ R6 → R7 → …（嚴格遞增）→ 待辦統計 → 變更紀錄（封存）
```

新 section 表格欄位統一：`| # | 項目 | 說明 | 狀態 |`（「範圍」「來源」等併入說明欄；歷史 section 不回溯改）。

編號：`{Section}-{序號}`（R13-1、P0-3），從 1 連續遞增不跳號，不嵌套優先級（不用 `R7-P0-1`）。

## PROGRESS.md §9 變更紀錄格式

```markdown
### YYYY-MM-DD 簡短標題

- ✅ **粗體摘要**：細節說明（一句話）
```

- 新條目加在 §9 最上方。
- **一主題一 `###` 條目**：同日多個獨立主題各開一個 `###`；同一主題同日多次修補才合併。

## 封存規則（live 檔瘦身，對應 `MAINTENANCE.md` §4 精簡）

TODO.md / PROGRESS.md 是持續累積的追蹤檔，會自然膨脹。**封存 = 逐字移動到 `docs/archive/`，不是刪除、不改內容**（弱模型勿把封存當清空）。

- **TODO.md**：整輪全部 `[x]`（0 未完成項）的輪次 → 移到 `docs/archive/TODO_done.md`（保持原順序、逐字保留；同檔累積追加）。live 檔只留「含未完成項的輪次」+「待辦統計」+「禁止事項」。輪次 R 編號因此不連續屬正常（新增輪次取號時，max 要一併查 archive）。
- **PROGRESS.md §9 切點**：挑一個明確的 `### YYYY-MM-DD` 當切點（保留最近約 2 個月），把該日期**之前**的條目整批移出。切點日期要同時寫進 live 的指標行與 archive 的表頭（本次切點：`2026-05-15`）。live 檔留 §1-8 概覽 + 近期日誌，並在 §9 末尾留一行指標指向 archive。
- **封存檔命名**：`docs/archive/CHANGELOG_{YYYY}{H1|H2}.md`（H1 = 1–6 月、H2 = 7–12 月，依被封存條目所屬半年；同半年累積同一檔），例 `CHANGELOG_2026H1.md`。
- **觸發時機**：live 檔 > 200KB（Read 工具單檔上限 256KB），或使用者要求。
- **允許的差異（僅限這些，其餘必逐字相同）**：(a) live 檔更新後的 preamble ＋ 指向 archive 的指標行；(b) archive 檔開頭新增的表頭（註明來源檔、日期/範圍、指回 live）。
- **驗證方法（強制）**：以「舊版 vs（live ＋ archive）」做**逐行 multiset 比對** —— 舊版有但新版缺 = 0；新版多出的只能是上一點的 scaffolding 行。改制度檔前先備份（`MAINTENANCE.md` §1）。

## DESIGN.md Decisions Log 格式

`| Date | Decision | Rationale |`，新決策加在表頭之後（反向時間序）。僅記設計/架構決策。

## 交付物受眾（寫任何文字前先確認給誰看）

| 交付物 | 受眾 | 語調 |
|---|---|---|
| Commit message / PR description | 團隊開發者 | 技術細節直寫，不解釋基礎 |
| PROGRESS.md / TODO.md | 專案負責人 + 未來的自己 | 半技術，重「做了什麼+為什麼」 |
| UI 文字 / 錯誤訊息 | 終端使用者（非工程師） | 白話中文，避免術語 |
| API error response | 前端開發者 | 結構化 JSON，訊息可直接顯示 |
| CLAUDE.md / docs 規範 | AI assistant + 維護者 | 精確、無歧義、可機械執行 |
