# Health Check - 2026-03-29

| 項目 | 狀態 | 備註 |
|------|------|------|
| cargo check | ✅ | 編譯成功，無錯誤 |
| cargo clippy | ✅ | 0 warnings |
| cargo deny | SKIPPED | 工具未安裝 |
| npm run build | ✅ | 建置成功，有 1 個循環依賴警告（見下方） |
| npm audit | ✅ | 0 漏洞 |
| tsc --noEmit | ✅ | 0 errors |
| migrations | ✅ | 001–023，連續無跳號 |

## 需要關注

- **Vite 循環依賴警告**：`src/lib/api/client.ts` 中的 `confirmPassword` 透過 `src/lib/api/index.ts` 重新匯出，兩個模組形成相互依賴，被 Rollup 分到不同 chunk，可能導致執行順序問題。建議在 `useUserManagement.ts` 中直接從 `src/lib/api/client.ts` 匯入，而非透過 `index.ts`。
