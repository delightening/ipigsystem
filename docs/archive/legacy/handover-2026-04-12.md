# 交接文件 — 2026-04-12

## 系統現況

- **正式環境**：ipigsystem.asia（Docker Compose，9 個容器全 healthy）
- **目前分支**：`main`（最新 commit：`1befbe0`）
- **整體完成度**：所有功能模組 100%，目前處於安全強化 + 維運優化階段

---

## 今天（2026-04-12）做了什麼

### 1. 修復行事曆日期偏移 bug
**檔案**：`backend/src/services/google_calendar.rs`
**問題**：Google Calendar 事件查詢的時間範圍用了 UTC，但台灣是 UTC+8，導致隔天（4/13）的事件跑到今天（4/12）顯示。
**修法**：改用 `TAIWAN_OFFSET_SECS` (UTC+8) 建構查詢範圍，精確對應台灣當日 00:00–23:59。
**commit**：`a18a08a`

### 2. 修復 CI cargo deny 安全掃描失敗
**檔案**：`backend/deny.toml`
**問題**：新發布的 `RUSTSEC-2026-0097` 報告 `rand 0.8` 有 unsound 問題，CI 因此失敗。
**修法**：確認我們未啟用觸發條件的 `log` feature，加入 ignore 清單。
**commit**：`1befbe0`

---

## 近期（本對話）完成的重要工作

| 日期 | 工作 | commit |
|------|------|--------|
| 4/10 | 第二輪 code review 修復（密碼強度、entity_type 400 錯誤） | — |
| 4/10 | 第三輪深度 code review 修復（7 項：IDOR、快取失效、輸入驗證等） | `f6aaa2b` |
| 4/10 | 修復 image-processor p-limit ESM 衝突、prometheus bcrypt 佔位符 | `b8e78ea` |
| 4/10 | 補上測試環境 AppState 缺少的 permission_cache 欄位 | `898885f` |
| 4/10 | file-upload 元件 blob URL 記憶體洩漏修復 | `2eeae53` |
| 4/10 | 合併 6 個 Dependabot PR（Cargo patch + npm patch/react-hook-form） | `a4584ef` 以前 |
| 4/12 | 行事曆時區 bug 修復 | `a18a08a` |
| 4/12 | cargo deny RUSTSEC-2026-0097 忽略 | `1befbe0` |

---

## 尚未處理的 Dependabot PR（8 個）

### Cargo（後端）
| PR | 套件 | 問題 | 建議 |
|----|------|------|------|
| #118 | hmac 0.12→0.13 | `jsonwebtoken v10` 要求 `hmac ^0.12.1`，升級會衝突 | ❌ 先不動，等 jsonwebtoken 更新 |
| #116 | sha2 0.10→0.11 | 同上，hmac/sha2 是同一組 | ❌ 先不動 |
| #115 | rust_decimal 1.40→1.41 | 未測試 | 可安全合併 |
| #114 | uuid 1.21→1.23 | 未測試 | 可安全合併 |

### npm（前端）
| PR | 套件 | 問題 | 建議 |
|----|------|------|------|
| #127 | react-i18next 16→17 | **major 升級**，CI 失敗，可能有 breaking change | ⚠️ 需測試 |
| #126 | @tanstack/react-query 5.90→5.96 | minor，但 CI 失敗（可能是 lock 衝突） | 可嘗試 |
| #125 | react-router-dom 6→7 | **major 升級**，路由 API 大改，CI 失敗 | ❌ 需大量測試 |
| #122 | i18next 25→26 | **major 升級**，API 有變化 | ⚠️ 需測試 |

---

## 已知技術債（低優先）

1. **`rand` 升級到 0.9**：可修復 RUSTSEC-2026-0097 根本問題。`thread_rng()` 改名為 `rng()`，需改 `ai_auth.rs`、`two_factor.rs`、`equipment.rs` 三個檔案。目前已用 ignore 暫時處理。

2. **`react-router-dom` 6→7 升級**：major breaking change，路由寫法有大改動，需要系統性重構，不是簡單合併 PR。

---

## 系統架構快速參考

```
Nginx (web)  ←→  Axum API (api)  ←→  PostgreSQL (db)
                      ↓
              Gotenberg (PDF)
              image-processor (圖片)
              Prometheus + Grafana (監控)
              db-backup (每日備份)
```

- **後端**：Rust / Axum，`backend/src/`
- **前端**：React / TypeScript / Vite，`frontend/src/`
- **部署**：`docker-compose.yml`，建置 push 到 main 自動觸發 CD
- **CI**：`.github/workflows/ci.yml`（cargo check / cargo test / eslint / e2e / cargo deny / cargo audit）

---

## 下次接手的建議起點

1. 先看 CI 是否綠燈（目前最新 commit 的 CI 還在跑）
2. 如要合 Dependabot，先從 **rust_decimal #115** 和 **uuid #114** 這兩個最安全的開始
3. **react-router-dom 6→7** 升級是一個獨立的大工程，需要專門規劃
