# iPig 系統：描述文件與實體代碼一致性分析報告

根據對 `c:\System Coding\ipig_system\` 目錄及其子目錄的深入調查，以下是針對 `描述/` 目錄下的軟體需求規格書 (SRS) 是否符合「現況」的分析結果。

## 1. 核心矛盾：技術架構衝突 (Critical)
這是最重大的不一致點。

*   **描述文件 (SRS) 宣稱**：系統採用 **Laravel + Livewire** 框架開發（參考 `描述/00_Index.md` 第 15 行）。
*   **實體專案現況**：系統完全基於 **Rust (Axum)** 後端與 **React (Vite/TS)** 前端構建（參考 `backend/` 與 `frontend/` 目錄結構、`README.md` 以及 `CLAUDE.md`）。
*   **結論**：描述文件所定義的技術實作方式與目前代碼庫**完全不符**。

## 2. 資料庫遷移 (Migrations) 數量不一
*   **描述文件 (SRS)**：提到參考了 19 個 PostgreSQL 遷移檔案。
*   **README.md**：亦宣稱有 19 個遷移檔案。
*   **實體現況**：`backend/migrations/` 目錄下實際僅有 **15 個** 檔案（編號 001 至 014，含 013 之後的小更新）。
*   **結論**：文件所描述的資料庫進度領先於目前實體環境，或參考了不同分支的資料。

## 3. 功能需求與業務規則 (FR/BR)
*   **描述文件 (SRS)**：提供了極其詳盡的編號系統（如 `FR-AUTH-001`、`BR-AUP-01` 等），並定義了多層審查狀態機（5.3 節）。
*   **實體現況**：代碼中（如 `backend/src/models/`）確實存在類似的角色與狀態邏輯，但 SRS 文件的細節（如 2FA 的特定速率限制 30 次/分鐘）在現有 Rust 代碼中可能尚未完全對齊或以 different 方式實作。
*   **結論**：SRS 文件更像是一份**重構計畫書**或**未來目標規格**，而非對現有代碼的「現狀描述」。

## 4. 文件版本與時間序
*   **README.md** 最後更新：2026-03-01 (v2.0)
*   **SRS 文件** 日期：**2026-03-16** (v1.0 草稿)
*   **推論**：由於 SRS 文件的日期較新，且其 1.1 節提到是「以 Laravel + Livewire 框架改寫之完整功能需求」，可以判定這組文件是**正在規劃中的改寫計畫規格**。

---

## 總結建議

如果您所謂的「現況」是指**目前正在運行的 Rust 系統**：
> [!WARNING]
> **描述文件不符合現況**。它描述的是另一個（或尚未實現的）Laravel 版本系統。

如果您所謂的「現況」是指**即將進行的改寫計畫**：
> [!NOTE]
> 描述文件可能是適用的，但它參考的資料庫遷移基底（19 個）比目前目錄下的（15 個）更多，需確認遺失的 4 個遷移內容。

### 建議行動：
1. **確認重構意向**：若不打算遷移至 Laravel，應修正 `描述/` 目錄下的技術描述。
2. **同步 Migrations**：確認為何實體檔案只有 15 個而文件宣稱 19 個。
3. **更新 README**：目前 `README.md`宣稱 19 個遷移，應調整為實際的 15 個。
