[package]
name = "erp-backend"
version = "0.1.0"
edition = "2021"
default-run = "erp-backend"
description = "iPig ERP 後端服務"
license = "MIT"

[dependencies]
# Web Framework
axum = { version = "0.7", features = ["macros", "multipart"] }
http-body-util = "0.1"
axum-extra = { version = "0.12", features = ["typed-header"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace", "request-id", "set-header"] }
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
async-trait = "0.1"

# Database
sqlx = { version = "0.9", default-features = false, features = ["runtime-tokio", "postgres", "uuid", "chrono", "rust_decimal", "json", "macros", "migrate"] }

# Auth
jsonwebtoken = { version = "11", default-features = false, features = ["rust_crypto", "use_pem"] }
argon2 = "0.5"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Validation
validator = { version = "0.21", features = ["derive"] }

# OpenAPI
utoipa = { version = "5", features = ["axum_extras", "uuid", "chrono", "decimal"] }
utoipa-swagger-ui = { version = "8", features = ["axum"] }

# Utilities
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
rust_decimal = { version = "1", features = ["serde"] }
thiserror = "2"
anyhow = "1"
dotenvy = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Email
lettre = { version = "0.11", features = ["tokio1-native-tls", "builder", "smtp-transport"] }

# Additional utilities
regex = "1"
rand = "0.10"

# Excel generation
rust_xlsxwriter = "0.97"

# Excel/CSV parsing
calamine = { version = "0.36", features = ["chrono"] }
csv = "1.3"

# Scheduler
tokio-cron-scheduler = "0.15"

# Hashing for migration resolution and audit integrity
sha2 = "0.11"
hmac = "0.12"
hex = "0.4"

# R66-B2/C6: app 層 AEAD at-rest 加密（TOTP secret / 簽章 payload）
chacha20poly1305 = "0.11"
zeroize = { version = "1", features = ["alloc"] }

# TOTP 2FA
totp-rs = { version = "5", features = ["otpauth", "qr", "serde_support", "gen_secret"] }

# GeoIP
maxminddb = "0.30"

# Zip (IDXF 分檔匯出/匯入)
zip = { version = "8.6", default-features = false, features = ["deflate"] }

# HTTP Client for external APIs
reqwest = { version = "0.12", features = ["json", "rustls-tls", "multipart"] }
url = "2"
base64 = "0.23"
urlencoding = "2.1"

# Prometheus Metrics
metrics = "0.24"
metrics-exporter-prometheus = { version = "0.18", default-features = false, features = ["http-listener"] }
dashmap = "6.2.1"
# H2: 權限快取 single-flight + TTL（取代 DashMap 手刻 cache）
moka = { version = "0.12", features = ["future"] }

# R30-3a: outbox worker 並行處理 batch（for_each_concurrent）
futures = "0.3"

# HTML sanitization (MCP email body)
ammonia = "4"

[dev-dependencies]
tokio-test = "0.4"
reqwest = { version = "0.12", features = ["json", "cookies"] }
serial_test = "4"
proptest = "1"
jsonwebtoken = { version = "11", default-features = false, features = ["rust_crypto", "use_pem"] }
chrono = { version = "0.4" }

[[bin]]
name = "create_admin"
path = "src/bin/create_admin.rs"

[[bin]]
name = "create_guest"
path = "src/bin/create_guest.rs"

[[bin]]
name = "healthcheck"
path = "src/bin/healthcheck.rs"

# R30-3a: outbox worker — poll event_outbox + send via ChannelRegistry + retry
[[bin]]
name = "outbox_worker"
path = "src/bin/outbox_worker.rs"

# 安全強化：Release 模式下也檢查整數溢位
[profile.release]
overflow-checks = true
