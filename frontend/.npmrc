# 供應鏈防護：不執行任何安裝期 lifecycle script。與 repo root 的 .npmrc 同一理由
# （2026-08-04 Shai-Hulud 蠕蟲，惡意碼藏在 npm install 的 hook 裡竊取憑證）。
#
# 這裡是縱深防禦，不是唯一防線：本目錄用 pnpm 10（見 package.json 的 packageManager），
# pnpm 官方文件載明「Since v10, pnpm doesn't run the lifecycle scripts of dependencies
# unless they are listed in onlyBuiltDependencies」，而本專案未設該允許清單 ＝ 預設全擋。
# 本檔補的是另一個缺口：**有人在這個目錄改用 npm**（歷史文件曾誤寫 `npm install`，
# 已於同一 commit 修正）。npm 不吃 pnpm 的那條預設，root 的 .npmrc 也涵蓋不到子目錄
# （實測：於 frontend 下 `pnpm config get ignore-scripts` 讀到 undefined），故需獨立一份。
#
# 安全性副作用：本目錄的 package.json 目前沒有 prepare / postinstall 等安裝期 script，
# 因此本設定不影響任何既有流程。已實測 pnpm 10.33.0：`pnpm run <script>`（CI 用的
# `pnpm run build`）與 `pnpm exec <bin>`（`pnpm exec playwright install`）皆不受影響。
# 日後若真的需要某個依賴跑編譯期 script，改用 pnpm 的 onlyBuiltDependencies 具名允許，
# 不要整份移除本設定。
ignore-scripts=true
